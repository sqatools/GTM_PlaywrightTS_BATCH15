//program to verify the person is allowed to enter movie with age >=18 and having a ticket

var PersonsAge =10;
var ticket = true;

if(PersonsAge>=18 && ticket==true)
{
    console.log("person can enter to movie");
}
else{
    console.log("person not allowed to enter to movie")
}